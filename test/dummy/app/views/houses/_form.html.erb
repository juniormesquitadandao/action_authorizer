<%= form_for(@house) do |f| %>
  <% if @house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@house.errors.count, "error") %> prohibited this house from being saved:</h2>

      <ul>
      <% @house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :street %><br>
    <%= f.text_field :street %>
  </div>

  <div class="field" <% if @house.new_record? %> style="display: none" <% end %>">
    <%= f.label :user %><br>
    <%= f.text_field :user, disabled: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
